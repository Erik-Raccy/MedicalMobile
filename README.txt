MedicalMobile App Project Files

Project can be opened using MedicalMobile.apk on an android device or by running the code through the project file
on Android ADT.

There are 5 types of users on the app: Administrators, Doctors, Nurses, Secretaries and Patients

With credentials respectively ("Username", "Password"): ("Admin", ""), ("Doctor", ""), ("Nurses", ""), 
("Secretary", ""), ("Patient1", ""), ("Patient2", "")